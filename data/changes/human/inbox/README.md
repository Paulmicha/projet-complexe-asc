# human change inbox

Dated nestable change YAML: `YYYY/MM/DD/<id>.yml` (+ optional `<id>/` children).

Required keys: `goal` (synonym `objective`), `paths`, `why`.

Specimen: `2026/07/27/foobar.yml`.
